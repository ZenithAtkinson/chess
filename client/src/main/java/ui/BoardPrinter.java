package ui;

public class BoardPrinter {

    // Needs to print an ASCII chess board, line by line
        // Make sure I am taking in a ChessGame object. For now, should just be a default state (see specs)
        // Should also be able to print out where all the given pieces are on the board inside that ChessGame object
            // (needs to be done for phase 6 anyway)


}
